import React from 'react';
import { Box } from '@mui/material';
import MediaType from '@/types/tmdb/IMediaType';

interface UpdateWatchRecordFormProps {
  // type: MediaType;
  // id: number;
  // poster_path: string | null;
  // title: string;
  // year: string;
  // totalEpisodes?: number;
}
const UpdateWatchRecordForm: React.FC<UpdateWatchRecordFormProps> = () => {
  return <Box>Form here</Box>;
};

export default UpdateWatchRecordForm;
